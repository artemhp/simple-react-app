import React from 'react';
import PropTypes from 'prop-types';

List.propTypes = {

};

function List(props) {
    return (
        <div>
            List
        </div>
    );
}

export default List;